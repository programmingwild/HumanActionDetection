# Default (slim, CPU-only): Streamlit Cloud free tier + local CPU runs.
# Torch-free inference via onnxruntime (~400MB RAM total).
# For GPU training locally, use requirements-gpu.txt instead.
streamlit>=1.30
onnxruntime>=1.17
numpy<2
pillow>=10.0
opencv-python-headless>=4.8
pandas>=2.0
